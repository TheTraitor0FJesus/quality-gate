name: Quality gate

on:
  workflow_call:
    inputs:
      python-version:
        required: false
        default: "3.11"
        type: string

jobs:
  quality-gate:
    name: quality-gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ inputs.python-version }}
      - run: python -m pip install --upgrade pip
      - run: python -m pip install "quality-gate[tools] @ git+https://github.com/TheTraitor0FJesus/quality-gate.git@v1"
      - run: quality-gate install-dependencies
      - run: quality-gate check
