waivers = []

[quality]
schema = 2
policy_release = "v2.0.1"

[repository]
name = "multi-component"
domains = ["repository", "python"]
required_documents = ["quality-gate.toml"]

[repository.limits]
max_blob_size_mib = 5

[repository.defaults]
command_timeout_seconds = 120
test_timeout_seconds = 300
gate_timeout_seconds = 600

[[python]]
name = "first"
path = "app1"
python_version = "3.12"
dependency_inputs = []
test_paths = []
tests_applicable = false
tests_reason = "Fixture checks per-component runtime reporting."
timeout_seconds = 300

[[python]]
name = "second"
path = "app2"
python_version = "3.12"
dependency_inputs = []
test_paths = []
tests_applicable = false
tests_reason = "Fixture checks per-component runtime reporting."
timeout_seconds = 300
