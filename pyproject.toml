[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "quality-gate"
version = "1.0.0"
description = "One quality contract for local Git hooks and GitHub CI."
requires-python = ">=3.11"

[project.optional-dependencies]
tools = [
    "mypy==1.19.1",
    "pytest==9.0.3",
    "ruff==0.15.12",
]
test = ["pytest==9.0.3"]

[project.scripts]
quality-gate = "quality_gate.cli:main"

[tool.setuptools]
packages = ["quality_gate"]

[tool.setuptools.package-data]
quality_gate = ["policy/*.toml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
extend = "quality_gate/policy/ruff.toml"

[tool.mypy]
files = ["quality_gate", "tests"]
