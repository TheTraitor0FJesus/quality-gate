# Release parity fixture

This fixture exercises the Python, web, and supplemental-test declarations used by the
release-backed Windows/Linux parity job.

The Python component is rooted at `app`, and the web component is rooted at `web`.
